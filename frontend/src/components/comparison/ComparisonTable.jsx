export default function ComparisonTable({ quotes = [] }) { const lowest = Math.min(...quotes.map((quote) => quote.price)); return <div className="panel comparison"><table><thead><tr><th>Supplier</th><th>Price / unit</th><th>Delivery</th><th></th></tr></thead><tbody>{quotes.map((quote) => <tr key={quote.id || quote.supplier}><td><strong>{quote.supplier}</strong><br /><small>{quote.location} {quote.verified && '· Verified'}</small></td><td className={quote.price === lowest ? 'best' : ''}>${quote.price.toFixed(2)}</td><td>{quote.lead}</td><td><button className="btn btn-quiet">Request quote</button></td></tr>)}</tbody></table></div>; }
