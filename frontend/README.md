# Buildwise frontend

A React + Vite interface for the Construction Material Price Comparison Portal.

## Run locally

```bash
npm install
npm run dev
```

The Vite app runs on port 5174. Set `VITE_API_URL` in `.env` to point at the backend API. The initial material directory uses local demo data so the procurement workflow remains previewable without a running API.
